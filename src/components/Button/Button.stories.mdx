import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, select, text } from '@storybook/addon-knobs';
import Button from './Button';

<Meta title="Jupiter|Button" component={Button} />

# Button

## Primary

Default, has priority.

<Preview>
  <Story name="Primary">
    <Button />
  </Story>
</Preview>

## Secondary

Used with primary for lesser priority CTA.

<Preview>
  <Story name="Secondary">
    <Button theme="secondary" />
  </Story>
</Preview>

## Props

<Props of={Button} />

## Playground

Use the "canvas" tab to try different prop values.

<Story name="Playground" parameters={{ decorators: [withKnobs] }}>
  <Button theme={select(`Theme`, [`primary`, `secondary`])} children={text(`Text`, `Learn More`)} />
  
</Story>
