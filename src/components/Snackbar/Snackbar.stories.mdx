import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Snackbar from './Snackbar';
import data from '../../data';

<Meta title="Jupiter|Snackbar" component={Snackbar} />

# Snackbar

<Preview>
  <Story name="Snackbar">
    <Snackbar>This will have some info.</Snackbar>
  </Story>
</Preview>

<Props of={Snackbar} />
